<%= render "shared/menu" %>

<main>
  <h2>Olá, <%= current_user.name %></h2>

  <table>
    <tr class="header-table">
      <th>#</th>
      <th>Palpiteiro</th>
      <th>Pontos</th>
      <th>CB</th>
      <th>VG</th>
      <th>V</th>
      <th>G</th>
    </tr>
    <% @users.each_with_index do |user, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><%= user.name %></td>
        <td><%= 0 %></td>
        <td><%= 0 %></td>
        <td><%= 0 %></td>
        <td><%= 0 %></td>
        <td><%= 0 %></td>
      </tr>
    <% end %>
  </table>
</main>